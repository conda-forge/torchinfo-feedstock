{% set name = "torchinfo" %}
{% set version = "1.6.6" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 8eb0a38c7dc2403b8f1eab101eb7cca012fb2eaf33caf248acb67c996836b70e

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - python >=3.6
    - pytorch >=1.6

test:
  imports:
    - torchinfo
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/tyleryep/torchinfo
  summary: Model summary in PyTorch, based off of the original torchsummary.
  license: MIT
  license_file: LICENSE
  doc_url: https://github.com/TylerYep/torchinfo#documentation

extra:
  recipe-maintainers:
    - TylerYep
    - sarthakpati
